# Progress report - 2026-08-29 11:28 ET

## Summary

This extended checkpoint closed two high ingestion defects and six medium contract/config/product findings: delivery-scoped recovery identity, per-item capability/scope authority, exact partial masks, fail-before-state configuration, truthful worker degradation, and linked fact provenance. Five focused commits landed. The last known medium—source revision and equal-time ordering—is implemented as migration `0016` and remains under independent review. The checkpoint is late because that review exceeded its expected window; hourly reporting has been resynchronized from 11:28.

## Metrics

| Metric | Result |
|---|---:|
| Full-suite evidence | 349 passed, no warnings |
| Temporal focused evidence | 92 passed |
| Branch coverage | 88%, CI floor 80% |
| Commits since 10:03 | 5 |
| CLI commands | 6 |
| HTTP routes | 11 plus static mount |
| Current Windows full-suite runtime | 11.61 seconds without coverage |
| Last Ubuntu/WSL parity | 335 tests, 88%, clean audit/install smoke |
| Tracked SQLite schema | Migration `0016` under review |
| Real ignored SQLite | Migration `0015`; application ID `ROOK`; integrity `ok`; 0 FK violations |
| Distribution | 78 sdist entries; 57 wheel entries; 26 installed runtime assets |
| Murmuration | 11:27 tend read-only; native profile still unavailable |

## Completed

- Required every facet and relationship item to prove exact action or unambiguous incidental capability/scope authority before identity, journal, or projection writes (`b06dc1a`).
- Added collection membership topology proof, adapter namespace and predicate checks, direct exact-target enforcement, absence authority, and action/incidental adversarial regressions.
- Scoped action batch and evidence identities to immutable deliveries, preserving retry attempt identity while allowing crash recovery (`ddabc73`, prior checkpoint).
- Enforced exact payload/mask equality and defensive masked merging for partial/PATCH evidence (`d81c8ad`).
- Added a 50 ms poll floor and shared profile grammar to TOML plus programmatic settings; invalid profiles now fail before database creation (`5515032`).
- Rejected redirected `serve` before config loading/runtime composition, proving refused output leaves durable configuration unchanged.
- Split successful cached reads with unavailable refresh from true local disconnection; warning/status copy now says the cached snapshot loaded (`99c7e1a`).
- Made standalone and source-checkout setup peer README paths.
- Joined supporting facet observations to bounded adapter/capability/version provenance and optional producing-action links without additional queries (`d7ce26d`).
- Validated action provenance joins by action/system/binding/adapter context; malformed IDs fall back to unlinked adapter provenance.
- Added migration `0016` for received-time projection order and implemented numeric source-revision precedence plus exact-tie first-accepted behavior.
- Completed the 11:27 Murmuration tend; no scoped public context appeared and credential-gated surfaces remained untouched.

## Validation and review

All committed slices passed independent skeptical review after adversarial repair rounds. The committed full suite passes 349 tests; migration `0016` passes 92 focused ingestion/storage/composition tests, including reverse received order, exact ties, source-revision downgrade rejection, legacy membership migration, and complete-boundary behavior. The exact package/coverage gate and real local migration are intentionally waiting for temporal review clearance. No live Databricks authentication or inventory was probed.

## Active work and ETA

Independent review of migration `0016` is the sole known medium-or-higher gate. After clearance: exact Windows/Ubuntu parity, recoverable real-database migration, coherent commit, then fresh residual audits. Work continues through Sunday 22:00 ET; the next tend and progress report are due around 12:27 ET.
