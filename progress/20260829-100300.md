# Progress report - 2026-08-29 10:03 ET

## Summary

This hour closed every known release-hardening finding, added a complete checkout-free first run, made installed-wheel verification portable to Ubuntu system Python and Snap uv, and repaired action redelivery identity after crash recovery. Five focused commits landed. The suite is green at 336 tests and 88% branch coverage; a fresh adversarial audit has now opened one high ingestion-authority defect and eight medium config/projection/product findings.

## Metrics

| Metric | Result |
|---|---:|
| Tests | 336 passed, no warnings |
| Branch coverage | 88%, CI floor 80% |
| Commits since 09:04 | 5 |
| Coordinator branch coverage | 94% |
| CLI commands | 6 |
| HTTP routes | 11 plus static mount |
| Current Windows test runtime | 10.92 seconds without coverage |
| Last Ubuntu/WSL parity | 335 tests, 88%, clean audit, installed-wheel smoke |
| SQLite | Migration `0015`; application ID `ROOK`; integrity `ok`; 0 FK violations |
| Distribution | 78 sdist entries; 57 wheel entries; 26 installed runtime assets |
| Locked audit | 27 packages, no known vulnerabilities |
| Murmuration | 09:36 tend read-only; native profile still unavailable |

## Completed

- Hash-pinned Hatchling 1.32.0 and all five transitive build requirements with a generated universal constraint set (`a64bafc`).
- Required hashed constraints for both sdist and sdist-to-wheel stages in CI and operator verification.
- Proved corrupted hashes fail immediately, regenerated constraints match, repeated Windows builds are byte-identical, and Windows/WSL archive member names/payloads match despite platform container metadata.
- Added `init-config` as a sixth CLI command with UTF-8, no-overwrite, parent creation, exact template parity, relative SQLite semantics, and two-writer race proof (`ccdde89`).
- Added a complete standalone `uv tool install → init-config → init → doctor → serve` workflow plus upgrade/uninstall guidance.
- Extended the isolated wheel verifier to execute checkout-free `init-config` and `init`, proving the generated config and SQLite database from the installed command.
- Replaced stdlib `ensurepip` dependence with uv-managed temporary venv/install; fixed Snap shim preservation by avoiding symlink dereference (`158162b`).
- Verified direct Ubuntu `/usr/bin/python3.12` with `/snap/bin/uv` and Windows both pass 78/57/26 installed-distribution checks.
- Completed the 09:36 Murmuration tend; no scoped public context appeared and all credential-gated surfaces remained untouched (`8dd6496`).
- Scoped Databricks batch and every normalized evidence ID to the immutable lease/delivery rather than the whole action (`ddabc73`).
- Added a real crash-after-ingestion recovery regression: second lease/delivery creates distinct batch/journal IDs, records zero ingestion issues, executes twice, and terminalizes correctly.

## Validation and review

Every release batch received adversarial independent review and at least one repair round. Windows and Ubuntu/WSL pass clean locked audits, format, standard/security lint, tests, constrained builds, complete 26-asset manifests, checkout-independent imports, and installed CLI initialization. Build constraints were regenerated and hash-corruption tested. Standalone installation was exercised through actual isolated `uv tool install`, upgrade, and uninstall while preserving operator files. No live Databricks authentication or inventory was probed.

## Active work and ETA

The remaining high finding is ingestion of facts outside any action or incidental capability/scope authority. Queued medium findings cover partial masks, source-revision/equal-time ordering, degraded-worker labeling, fact provenance, README hierarchy, minimum poll interval, shared profile validation, and redirected-serve preconditions. Work continues through Sunday 22:00 ET; the next Murmuration tend is due around 10:36 ET and the next progress report around 11:03 ET.
