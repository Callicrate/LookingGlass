# Rookery progress report: 2026-08-30 08:09 ET

## Outcome

This hour closed the strict profile/local-recovery review, committed it, and posted exact evidence to Murmuration topic 40 post 8. The next compatibility review then reproduced four medium defects: Python tool interpreter drift, unbounded CLI version certification, descendant process escape, and stale caller-time lease authority. Python and version certification are independently clear; process-tree and store-clock candidates are implemented and in final re-review.

## Metrics

| Metric | Result |
|---|---:|
| Last clean committed Windows full gate | 582 passed, 8 expected platform skips |
| Last clean committed Ubuntu/WSL full gate | 584 passed, 6 expected platform skips |
| Windows process-tree normal/timeout focus | 2 passed |
| Ubuntu/WSL process-tree normal/timeout focus | 2 passed |
| Storage plus runtime-stop integration | 100 passed, 7 expected platform skips |
| Local CLI doctor | Certified 0.298.0 passed exact version and seven leaf contracts |
| Python-pinned tool install/force-upgrade smoke | Passed in disposable Windows tool directories |
| Last rebuilt distribution candidate | 89 sdist entries, 66 wheel entries, 34 installed runtime assets |
| Locked dependency audit | 27 packages, no known vulnerabilities or adverse status |
| Real SQLite | `ROOK`; migration `0021`; integrity `ok`; 0 FK violations |

## Completed this hour

- Closed and committed the one-high/three-medium strict profile/local-recovery review as `6c041a2`, `cef4539`, `516c0cd`, and `d1148a9`; posted closure as exact-provenance Murmuration topic 40 post 8.
- Opened `critical-reviews/20260830-072928.md` for executable compatibility and upgrade safety.
- Pinned documented initial and force-upgrade tool installs to Python 3.12. Extended distribution verification with disposable `UV_TOOL_DIR`/`UV_TOOL_BIN_DIR` install, interpreter assertion, force-upgrade, second assertion, and installed CLI smoke. Independent re-review is clear.
- Replaced “0.298 or newer” with explicit certification of only CLI 0.298.0. Doctor now full-matches the version line, probes every reachable leaf usage and required profile/output/format flags, and rejects uncertified versions before leaf execution.
- Bound successful doctor to executable identity, size, high-resolution modification time, and SHA-256 before/after probes. Every mapped dispatch rehashes outside the event loop and recertifies changed bytes before profile snapshot or execution. Official same-path 1.10 replacement and 0.298 reinstall probes independently clear this boundary.
- Added complete CLI process-tree ownership: POSIX new session/process group; Windows suspended creation, kill-on-close Job assignment, thread resume only after assignment, Job termination and bounded drain before cleanup. Real long-lived descendants with inherited pipes and detached stdio now stop on timeout and normal completion.
- Added an injectable `SQLiteStore` clock. Action lease promotion, issuance/reclaim, liveness, heartbeat extension, start authorization, attempt/completion, satisfied terminalization, and action-linked ingestion use store time instead of stale caller timestamps.
- Updated stale runtime-stop and action-linked ingestion tests to advance the store clock explicitly; focused storage and integration tests are green.

## Review status and ETA

The Python tool and executable-version findings are independently closed. Process-tree re-review is rerunning the prior Windows assignment/cleanup race against suspended creation plus Job drain. Lease-clock re-review is tracing every action lifecycle and deadline path. After clearance: exact full Windows/WSL gates, rebuilt/repeated distributions, focused commits, ledger closure, and the next review. The autonomous campaign remains active through Sunday at 22:00 ET.
