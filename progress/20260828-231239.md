# Progress report - 2026-08-28 23:12 ET

## Summary

Two substantial checkpoints landed this hour. Bounded alert history was committed as `5f6acb1`, and the reproduced unauthenticated-loopback refresh path was closed and committed as `f6acbe3`. The next demonstrated defect, direct Workspace metadata observations being rejected for missing coverage, is already fixed in the working tree and passing its vertical normalizer-to-SQLite regression.

## Metrics

| Metric | Result |
|---|---:|
| Tests | 204 passed, no warnings |
| Branch coverage | 84%, CI floor 80% |
| HTTP routes | 9 plus static mount |
| Alert history | 50 rows per page, type/severity filters, 10,000-page ceiling |
| Local authorization | 256-bit session/bootstrap values, 10-minute single-use bootstrap |
| Unauthorized protected routes | 403 before parsing or backend work |
| Auth browser QA | Real HTTP exchange passed; fragment scrubbed; cookie hidden from JavaScript |
| Bootstrap Lighthouse | 100 in every audited category |
| Security lint / secret scan | Pass / no matches |
| Package build | Wheel and source distribution pass |
| Independent security review | No blocker or residual finding |
| Metadata vertical regression | Direct metadata refresh reaches `succeeded` and updates SQLite |

## Completed

- Added bounded, filterable full alert history without expanding the dashboard's ten-alert read.
- Added exact static SQL query shapes and recency indexes for all alert filter combinations.
- Added a single-use URL-fragment bootstrap and process-local browser session.
- Default-denied every cached-data and mutation route while keeping bootstrap/static assets public.
- Preserved Trusted Host, same-origin, CSRF, strict form parsing, and generic failure responses.
- Bound CSRF and durable manual-intent attribution to the authorized UI session.
- Prevented accidental activation-capability persistence through redirected stdout unless explicitly opted in.
- Verified the real browser flow, token-free access-log targets, package contents, full CI gate, and tracked-source secret scan.
- Added exact complete scope coverage, without absence authority, to direct Workspace metadata normalization.
- Added a real metadata request-to-normalizer-to-SQLite-to-intent regression.
- Removed an unauthorized generated Murmuration cache commit left by a read-only subagent; intentional history and the active metadata diff were preserved.

## Remaining boundary

Three demonstrated local defects remain in the evidence-ranked queue: delayed older complete evidence can hide a newer relationship, cancellation of a compatibility check can orphan its CLI subprocess, and rejected ingestion items can leave partial identity mutations. Live Databricks inventory remains intentionally credential-gated and was not attempted.

## Next actions

Close independent review and commit the metadata fix, targeted within the next 30 minutes. Then repair out-of-order relationship reconciliation, followed by cancellation-safe doctor subprocess cleanup. The next hourly report is due by 00:12 ET.
