# Rookery progress — 2026-08-30 16:20 ET

## Outcome this hour

Rookery now keeps healthy cached systems readable when one authority/configuration record is malformed, while removing every trust label, refresh option, and registration path for only the affected system. The same review repaired three history/cursor cost regressions introduced or exposed by readable containment views. Implementation `e5444ea` and evidence `9c55331` are committed; exact Windows/WSL behavior, real migration, rendered warning UI, independent review, and installed-wheel package gates are green.

## Metrics

| Measure | Current evidence |
|---|---|
| Windows full suite | 665 passed, 8 expected platform skips |
| Ubuntu/WSL full suite | 667 passed, 6 expected platform skips |
| Windows coverage | 90.08% statements / 76.44% branch-only / 86.77% combined |
| Ubuntu/WSL coverage | 87.54% statements / 75.23% branch-only / 84.56% combined |
| Static diagnostic probe | Pyright 1.1.411 remains 144; no suppression added |
| Package verification | 94 sdist entries / 70 wheel entries / 38 installed runtime assets on both OS families |
| Real ignored store | `ROOK`, 25 migrations, integrity `ok`, zero FK violations, 13 readable views, 7 containment triggers |
| Deep object cursor | 21 rows / 210 validators at 20,000 rows |
| Deep action cursor | 21 rows / 105 validators at 20,000 rows |
| Rendered warning UI | No overflow/console errors; Lighthouse 100 in all four audited categories |
| Murmuration | 16:16 Tend operational under exact `openai/gpt-5.6-sol`; no uncommitted report posted |

## Material changes

- Migration `0025_authority_read_plans` adds readable bindings, capabilities, scopes, identities, an action recency plan, and a trigger-maintained relationship type/cursor projection.
- Binding fingerprints require exact lowercase nonzero 64-hex; identity authority keys are recomputed from the readable fingerprint/workspace-root material.
- Ten corruption cases prove system A loses verified trust/options/registration while system B cache/detail pages remain healthy.
- The intentionally deferred content capability with an empty coverage-policy tuple remains valid but still does not appear when content capture is disabled.
- Unfiltered and filtered action cursors split same-time/tiebreak and older ranges instead of scanning with a mixed-direction `OR`.
- Object prefix queries use ASCII-NOCASE literal ranges; later pages split same-name and greater-name ranges, including uppercase `Z` correctness.
- Relationship type filters use `(subject, predicate, presence, object_type, object_id)` and materialize only the bounded page before child joins.
- Healthy relationships no longer trigger warning side effects; cross-system containment remains isolated and warned.
- A no-overwrite pre-0025 backup is retained at `.local/backups/rookery-20260830-161658-pre-0025.sqlite3`.

## ETA and next work

This review is complete. The campaign continues through 22:00 ET with another fresh residual review rather than a handoff. The next likely surface is operational configuration/apply atomicity under partially corrupt desired-state rows, followed by the next hourly Tend/progress checkpoint.
