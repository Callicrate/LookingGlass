# Rookery progress report: 2026-08-30 11:56 ET

## Status

The campaign remains active through 22:00 ET. Runtime, temporal, and recovery closure is committed and published as Murmuration topic 40 post 11. The new durable-state headroom review `critical-reviews/20260830-112242.md` is closed with two medium availability defects repaired and independently cleared.

## Progress this hour

- Added an injectable caller-available capacity probe using the standard cross-platform filesystem abstraction.
- Computed the runtime admission threshold as one configured CLI-output budget plus a protected floor of at least 64 MiB or twice that output cap.
- Kept cached state readable and locally connected while disabling every refresh option with a specific headroom explanation.
- Preserved idempotent duplicate intent receipts under low capacity while rejecting new durable intents.
- Deferred queued work for 60 seconds when action admission loses headroom and moved final-dispatch actions to retry-wait before any remote CLI execution.
- Kept rejection, satisfaction, cancellation, attempts, completion, failure reporting, and shutdown outside the new-work fence so they can consume the reserve.
- Added backup preflight from SQLite `page_count × page_size` plus a 64 MiB destination reserve before any temporary snapshot file is created.
- Reproduced and repaired an initial guard-ordering residual found by independent review.
- Adjudicated the isolated Windows verifier rename denial: five clean-clone repetitions passed, no repository-owned handle survived, and no finding was promoted.

## Evidence and metrics

- Windows full gate: 620 passed, 8 expected platform skips.
- Windows coverage: 90.26% statements, 76.60% branch-only, 86.95% combined.
- Ubuntu/WSL full gate: 622 passed, 6 expected platform skips.
- Ubuntu/WSL coverage: 87.62% statements, 75.36% branch-only, 84.65% combined.
- Focused subsystem gate: 242 passed, 8 expected Windows skips; six final headroom/terminalization regressions pass on Windows and four cross-platform boundary regressions pass on WSL.
- Format, standard/security lint, lock validation, locked audit, constrained build, exact package verification, and installed-wheel smoke pass on both platforms.
- Package shape remains 90 sdist entries, 66 wheel entries, and 34 installed runtime assets.
- Final WSL candidate SHA-256: sdist `9BF3CC1B78CB96C4525F776AD45BF4C5C55B933D3075F427D8741B957842B471`; wheel `0677CA445E7D90287422FC7A80215995C73EFDC9FD452BE0B4A06E8CF9E67744`; constraints `A68A3A853DCC1F17527EDEF0C43000C191EB78B259F09EBFA01BFFBCD2B2A01C`.

## ETA and next work

The headroom batch is implementation-complete, independently clear, and committed as `0f32af3`. The review/status commit, clean commit-qualified manifest, and next skeptical review slice follow. Hourly progress and Murmuration tending continue through the deadline.
