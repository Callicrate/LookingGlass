# Progress report - 2026-08-29 08:03 ET

## Summary

This hour closed a high stale-worker ingestion race, expired coordinator authority at transaction time, corrected current-versus-planned product documentation, and brought durable complete-membership watermarking through its first exact gate. Windows is current at 324 tests and 88% branch coverage. Three focused commits landed; migration `0015` remains uncommitted pending post-fix review of two repaired upgrade blockers.

## Metrics

| Metric | Result |
|---|---:|
| Tests | 324 passed, no warnings |
| Branch coverage | 88%, CI floor 80% |
| Commits since 07:05 | 3 |
| Coordinator branch coverage | 94% |
| CLI commands | 5 |
| HTTP routes | 11 plus static mount |
| Current Windows test runtime | 13.95 seconds under coverage |
| Last Ubuntu/WSL parity | 319 tests, 87%, before the current storage batches |
| Tracked SQLite schema | Migration `0015` under review |
| Real ignored SQLite | Migration `0014`; integrity `ok`; 0 foreign-key violations |
| Distribution | 75 sdist entries; 57 wheel entries on current Windows build |
| Murmuration | 07:40 tend read-only; native profile still unavailable |

## Completed

- Decoupled current manual refresh behavior from planned Phase 4 scheduling/subscription/UI prose and distinguished fail-fast `doctor` from supervised nonblocking `serve` (`7ce2c64`).
- Required first-time action-linked ingestion to prove the matching current, strictly unexpired `running` lease in the same immediate transaction (`ab7ca7e`).
- Preserved actionless incidental evidence, exact duplicate replay, and verified legacy duplicate backfill while preventing stale workers from reserving deterministic batch IDs.
- Independently exercised ten two-connection ingestion/reassignment races; stale attempts made no canonical or batch-ID mutation and replacement leases succeeded.
- Expired coordinator claims at fresh transaction-local decision time before every disposition or action attachment, including new, existing, and uniqueness-race paths (`4dd84cf`).
- Removed the resulting test-clock flake with a deterministic per-test store clock while retaining an exact-boundary expiry test.
- Added migration `0015` and runtime projection logic for relationship-authoritative complete-membership watermarks.
- Added deterministic legacy backfill, stale-projection reconciliation, invalid legacy target skipping, runtime target validation, restart persistence, delayed-edge suppression, and genuinely newer restoration coverage.
- Rejected an apparent Unity Catalog pagination defect after installed help and pinned v0.298 source proved the CLI iterator already exhausts all pages before rendering one JSON array.
- Completed fresh protected-web and release audits; the web surface cleared, while five concrete release-hardening findings entered the queue.
- Completed the 07:40 Murmuration tend; no scoped public context appeared and all credential-gated surfaces remained untouched.

## Validation and review

The current Windows gate passes lock validation, format, standard/security lint, 324 tests, 88% branch coverage, package builds, and archive verification. Independent reviews materially tightened both storage repairs: the action fence gained exact-expiry and race proof; membership migration gained legacy stale-row reconciliation plus invalid-target/FK defenses. Current Ubuntu/WSL parity predates these storage commits and will be refreshed after migration `0015` clears and commits. No live Databricks authentication or inventory was probed.

## Active work and ETA

Post-fix review of migration `0015` is running. Next are incompatible-database identity rejection and five release items: complete asset verification/install smoke, constrained build isolation, pytest advisory remediation plus CI audit, standalone-install guidance, and immutable CI action pins. Work continues through Sunday 22:00 ET; the next Murmuration tend is due around 08:40 ET and the next progress report around 09:03 ET.
