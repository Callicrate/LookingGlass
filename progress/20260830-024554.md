# Rookery progress — 2026-08-30 02:45 ET

## Status

The fresh 02:06 critical review is fully implemented and in final independent verification. Recovery and runtime-responsiveness batches are already committed; release-evidence, installed-documentation, and collection-truth changes are green locally and awaiting the focused sdist-manifest follow-up verdict before commit.

## Progress this hour

- Reconfirmed the repaired native Murmuration identity and verified its recovery follow-up at topic 40, post 2.
- Published and committed a fresh first-look critical review with eight medium findings.
- Quarantined malformed leased/running/retry timing during store recovery, closing linked state and releasing action dedupe authority with idempotent alerts.
- Reduced event-loop SQLite contention to 250 ms after a separate 5 s startup serialization window.
- Added bounded runtime yields and repaired a pre-existing lost-wake race exposed by the timing change.
- Prevented CI from building the local project before the hash-constrained build step.
- Added explicit 85% statement, 75% branch-only, and 80% combined coverage floors.
- Bound the wheel's complete package tree, metadata, README, dependency declarations, RECORD entries, and runtime assets to current source bytes.
- Added exact sdist source-byte and manifest checks, unsafe archive-member rejection, and byte-identical constrained sdist-wheel reconstruction.
- Added `export-docs` and packaged the exact architecture contract for checkout-free wheel users.
- Changed containment language and evidence to state `unknown` collection completeness and “last-observed children,” avoiding false absence/current-set claims.
- Pinned `actions/upload-artifact` v7 by commit and retained each verified matrix archive pair for 14 days.

## Metrics

- Windows: 509 passed, 8 platform skips.
- Windows coverage: 91.49% statements, 77.57% branch-only, 88.16% combined.
- Ubuntu/WSL: 511 passed, 6 platform skips.
- Ubuntu/WSL coverage: 89.82% statements, 76.99% branch-only, 86.75% combined.
- Package: 86 sdist entries, 63 wheel entries, 31 installed runtime assets.
- Reproducibility: repeated constrained Windows builds are byte-identical; the directly built and sdist-rebuilt wheels are byte-identical on each platform.
- Current repeated Windows hashes before the final manifest-only change: sdist `75A1F3CBE0574797ABDEE0FE66EA603548784ED52EA43E599D5233643809EB99`, wheel `C4EA4FDE7CC8147DE19D982E2E611031EF88F133D51E3AD300E3310D34A4A3F3`.
- Lock audit: 27 packages, no known vulnerabilities or adverse project statuses.

## ETA and next work

The current remediation batch is approximately 15 minutes from focused commits and status closure, subject to the independent exact-sdist follow-up. The campaign itself has no early exit and continues through 22:00 ET. The next pass will target a fresh residual surface after these findings are closed.

## Risks / limits

- No live Databricks inventory was attempted without an explicit profile.
- Restore tooling, power-cut testing, authoritative multipart collection completeness, and external release publication remain explicitly deferred.
- A reviewer's apparent 60.76% branch result came from a partial changed-test run; both exact full-platform runs exceed the declared 75% branch-only floor.
