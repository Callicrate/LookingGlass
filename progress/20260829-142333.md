# Progress report - 2026-08-29 14:23 ET

## Summary

This hour closed one medium exact-expiry dispatch race and uncovered the next deep performance/identity tranche: one high oversized collection path and three remaining medium findings. The high now has a dual-reviewed architecture and a full local implementation; all 369 tests, 88% branch coverage, and package verification pass, but the commit is intentionally held for the original finder's final re-review. Two focused commits landed before that implementation.

## Metrics

| Metric | Result |
|---|---:|
| Current Windows full-suite evidence | 369 passed, no warnings |
| Last committed Ubuntu/WSL parity | 364 passed, 88% |
| Branch coverage | 88%, CI floor 80% |
| Commits since 13:23 checkpoint | 2 |
| Supported large collection regression | 10,000 children and relationships persisted |
| Multipart hard bounds | 1,000,000 canonical bytes and 250 linked units |
| Part-ingestion timing | Diagnostic only; deterministic byte/unit bounds are authoritative |
| CLI commands | 6 |
| HTTP routes | 11 plus static mount |
| Distribution | 80 sdist entries; 59 wheel entries; 28 installed runtime assets |
| Murmuration | 14:23 tend read-only; native project profile still unavailable |

## Completed

- Audited supported-scale SQLite and adapter behavior with million-row query-plan probes and maximum-size normalized collections.
- Made final dispatch reject `leased_until <= now` and atomically renew a successful running lease (`c227139`).
- Independently proved exact-expiry reclaim and just-before-expiry mutual exclusion in 40 two-connection races.
- Chose deterministic adapter-side multipart delivery after two independent architecture reviews; storage staging remains deferred until multipart complete-coverage reconciliation is required.
- Added one shared canonical observation-batch byte encoding used by both storage identity and adapter packing.
- Kept linked facet/`contains` evidence indivisible, sorted units canonically, moved the root membership summary to the final part, and kept observation IDs independent of chunk boundaries.
- Bounded every part by both bytes and statement-heavy units, then tightened the unit limit from 500 to 250 when the coverage gate exposed a 1.78-second instrumented transaction.
- Made `NormalizedResult.batches` authoritative, heartbeat/yield between parts, stop on lease loss or rejection, and record exactly one aggregate attempt/completion.
- Kept incomplete declarations on every part so `satisfies` claims remain valid; multipart `COMPLETE`/absence authority and oversized indivisible units fail before any ingestion call.
- Added deterministic reorder, oversized Unity Catalog unit, complete-coverage fail-closed, worker rejection/heartbeat-loss, and real 10,000-child SQLite regressions.
- Revalidated the exact Windows lock, audit, lint, 369-test coverage, build, and installed-wheel package gates.
- Completed the hourly Murmuration tend. No scoped discussion appeared; native identity, notifications, and BookStack remain unavailable, so no write was attempted.

## Validation and review

The high-severity implementation passes a real worker-to-SQLite 10,000-child path with all objects and edges present, the root count published last, and partial outcome truthfully retained. Same-delivery packing is deterministic under source reorder; complete/absence semantics cannot cross parts. Independent timing samples varied by instrumentation and host, so the 1,000,000-byte/250-unit limits—not a wall-clock assertion—are the stable gate. The final independent review is still running after the unit-bound tuning. No live Databricks authentication or inventory was probed.

## Active work and ETA

The high collection repair remains the active gate. After independent clearance it will be committed and revalidated on Ubuntu/WSL. The queued medium findings are stable Unity Catalog schema/relation/volume IDs, temporary full-backlog sorting in both lease selectors, and retained-history scans for current cooldown/dashboard action state. Work continues through Sunday 22:00 ET. The next tend and progress report are due around 15:23 ET.
