# Rookery progress report: 2026-08-30 09:59 ET

## Outcome

The standalone/runtime review expanded to eight medium findings under repeated supply-chain challenge. All have implemented candidates. Runtime health, offline prerequisite truth, exact dependency versions, official CLI origin, and ordinary manifest binding are independently clear. Hash-required dependency bytes, untracked-source rejection, and fresh-environment upgrade/rollback are passing on Windows and WSL and remain in the final re-review loop.

## Metrics

| Metric | Result |
|---|---:|
| Current Windows full gate | 598 passed, 8 expected platform skips |
| Current Ubuntu/WSL full gate | 600 passed, 6 expected platform skips |
| Windows coverage | 90.27% statements, 76.54% branch-only, 86.95% combined |
| Ubuntu/WSL coverage | 87.58% statements, 75.27% branch-only, 84.61% combined |
| Current release candidate | 90 sdist entries, 66 wheel entries, 34 installed runtime assets |
| Distribution unit suite | 14 passed |
| Locked dependency audit | 27 packages, no known vulnerabilities or adverse status |
| Official CLI origin | Windows/Linux 0.298.0 binary digests pinned before execution |
| Real SQLite | `ROOK`; migration `0021`; integrity `ok`; 0 FK violations |

## Completed this hour

- Opened `critical-reviews/20260830-090125.md` and reproduced standalone offline-prerequisite and post-start runtime-health gaps.
- Clarified that wheel installation requires an index, populated cache, or reviewed wheelhouse and is not a self-contained offline bundle.
- Split worker-global `CliIncompatible`/`CliRuntimeUnavailable` from profile-local `CliUnavailable`. Global failures record the action, disable controls, emit one event, retry doctor, and recover only after recertification; profile-local failures remain scoped. Independent re-review is clear.
- Added official x86-64 CLI 0.298.0 Windows and Linux binary digest gates before `--version`. The installed Windows binary byte-matched the checksum-verified official release and retained a valid Databricks Authenticode signature.
- Added tracked hash-bearing `runtime-constraints.txt` generated from `uv.lock` and changed standalone install to hash-required/no-build dependencies plus no-deps Rookery wheel.
- Added a corrupted Click-hash probe that must fail before dependency installation.
- Bound CI artifact names to full source commit and added clean-checkout commit-qualified SHA-256 manifests covering sdist, wheel, and constraints. Untracked files now make release publication fail rather than receive a false commit identity.
- Replaced in-place upgrades with fresh relocatable sibling environments, exact freeze/CLI smoke, recoverable rename swap, and retained prior environment. A synthetic prior-only package with executable `.pth` hook proves removed code cannot survive; corrupted-hash staging proves the active environment remains usable.
- Updated standalone checksum verification and upgrade/rollback instructions.

## Review status and ETA

The original runtime-health and offline-truth findings are independently clear. The final supply-chain reviewer has closed ordinary graph drift, commit naming, and official CLI origin, then found and received repairs for dependency-hash enforcement, untracked manifest contamination, and stale executable packages. Windows and WSL verifier passes are green; final clean-checkout/rollback adjudication is pending. Next: exact clean manifest, repeated artifacts, focused commits, ledger/Murmuration closure, then another review. The campaign remains active through Sunday at 22:00 ET.
