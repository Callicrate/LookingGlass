# Progress report - 2026-08-29 03:12 ET

## Summary

This hour closed four independently reproduced medium defects across lifecycle accounting, browser authorization, schema recovery, and configuration identity; then it added a consistent online backup command and corrected refresh-policy propagation. The user-facing product now consistently presents itself as Rookery while retaining the existing package and CLI compatibility name. All completed work is in focused commits and the tree is clean.

## Metrics

| Metric | Result |
|---|---:|
| Tests | 260 passed, no warnings |
| Branch coverage | 86%, CI floor 80% |
| Commits since 02:12 | 16 |
| CLI commands | 5 |
| HTTP routes | 11 plus static mount |
| Full local test runtime | 10.51 seconds |
| SQLite | Migration `0011`; integrity `ok`; 0 foreign-key violations |
| Package verification | Lock valid; wheel and source distribution build |
| Murmuration | 02:43 tend read-only; native profile still unavailable |

## Completed

- Expired coalesced requests now retain their own terminal truth while a still-live request may continue authorizing the shared action (`d08540a`).
- System cards prefer active work over later terminal history through a bounded partial index (`ac50fd3`).
- Poison-action ordering is deterministic in the integration regression (`f2396b7`).
- Browser sessions now enforce two-hour idle and twelve-hour absolute server-side lifetimes, clear stale cookies, and require fresh process activation (`1f0077b`).
- Startup repairs missing or semantically mismatched indexes required by forced dashboard plans, with atomic rollback proof (`d3b020d`).
- Shutdown cancellation now has an end-to-end reopen and lease-reclaim proof (`8a2b4bf`).
- Case-only configured-system ID edits preserve the original durable identity and cache, including mixed-case legacy rows (`de73946`).
- Unsupported Workspace content persistence now fails before target resolution or CLI execution and cannot claim freshness (`2818af9`).
- `backup --output <path>` creates an integrity-checked online SQLite/WAL snapshot, never overwrites, and handles publication races safely (`8f60c42`).
- The browser interface, README, package description, and favicon label now consistently use Rookery while `async-api-view` remains the stable compatibility surface (`12d233c`).
- Refresh-policy changes now wake deferred requests only for coordinator re-evaluation; migration `0011` also fixes nullable override identity (`0b59950`).
- The README setup path no longer embeds one machine-specific checkout (`a113604`).

## Validation and review

Independent reviewers cleared each consequential slice after focused reproductions. The final gate passed formatting, standard/security lint, lock validation, 260 tests at 86% branch coverage, and package builds. The ignored local database upgraded through `0011_refresh_override_identity` with SQLite integrity `ok` and no foreign-key violations.

The 02:43 Murmuration pass resolved the tracked project identity and leeway 50, found no Rookery-specific public discussion, and stayed read-only because writes, notifications, and BookStack remain blocked by the missing native project profile.

## Active work and ETA

A fresh documentation-truth audit is running against current behavior and roadmap claims. There is no artificial completion ETA before Sunday 22:00 ET; the campaign continues by resolving demonstrated risk, validating each slice, and committing it cleanly. The next Murmuration tend is due around 03:43 ET, and the next progress report is due by 04:12 ET.
