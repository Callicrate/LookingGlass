# Rookery progress — 2026-08-30 15:24 ET

## Outcome this hour

Rookery now contains malformed retained state instead of letting one row hide healthy cache or wedge later queue work. The review closed three high availability/queue failures and four medium detail/link/state/authority failures. Implementation `d313b53` and review evidence `15a1730` are committed; Windows, Ubuntu/WSL, the real local database migration, and exact installed-wheel verification are green.

## Metrics

| Measure | Current evidence |
|---|---|
| Windows full suite | 652 passed, 8 expected platform skips |
| Ubuntu/WSL full suite | 654 passed, 6 expected platform skips |
| Windows coverage | 90.35% statements / 76.88% branch-only / 87.08% combined |
| Ubuntu/WSL coverage | 87.75% statements / 75.64% branch-only / 84.81% combined |
| Static diagnostic probe | Pyright 1.1.411 remains 144; no suppressions or partial gate |
| Package verification | 93 sdist entries / 69 wheel entries / 37 installed runtime assets on both OS families |
| Locked dependency audit | No known vulnerabilities or adverse project statuses in 27 packages |
| Real ignored store | `ROOK`, 24 migrations, integrity `ok`, zero FK violations, 8 readable views, 4 NULL-ID triggers |
| Murmuration | 14:49 Tend operational under `project-a3c5f71ec6d12b` and exact `openai/gpt-5.6-sol`; no uncommitted claim posted |

## Material changes

- Queue quarantine now addresses corrupt rows by transaction-local SQLite `rowid`, gives NULL public IDs one surrogate UUID, and handles every active state without loops or starvation.
- Stored JSON decoding catches recursion and applies the shared depth/container/node contract before reconstruction.
- Startup reconciles malformed, dangling, missing, and contradictory intent/action links, restores truthful terminal history, rejects active divergence, and stops parentless active actions.
- Unknown retained states terminalize once instead of remaining invisible.
- Migration `0024_corruption_containment` adds NULL-ID triggers and presentation-only readable views for systems, objects, facets, relationships, action summaries/attempts/facet status, and events.
- Browser reads isolate malformed rows query-locally, show one accessible nonblocking warning, preserve healthy cache/history/detail pages, null corrupt optional alert links, and reject cross-system containment.
- Valid legacy cursor timestamps are canonicalized losslessly during migration; queue authority continues to require exact canonical timestamps.
- A no-overwrite pre-0024 backup is retained at `.local/backups/rookery-20260830-152043-pre-0024.sqlite3`.

## ETA and next work

This review is complete. The next fresh critical-review surface begins immediately; likely targets are residual authority/configuration read containment and migration/view query-plan cost. The autonomous campaign continues through 22:00 ET, with the next Tend/progress passes in about an hour.
