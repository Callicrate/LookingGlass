# Rookery progress report: 2026-08-30 10:51 ET

## Status

The campaign remains active through 22:00 ET. The standalone release review is committed and published through Murmuration topic 40 post 10. The maintainability and residual-failure review at `critical-reviews/20260830-102227.md` is now closed with one low and seven medium findings repaired and independently cleared.

## Progress this hour

- Repaired malformed `Origin` and `Referer` handling so parser failures return a controlled 403 with security headers instead of plaintext 500.
- Made bootstrap capability expiry exclusive at the exact deadline, matching session and durable deadline semantics.
- Added a final SQLite parent-intent expiry fence across rejection, satisfaction, deferral, coalescing, and admission, preventing post-expiry receipts and action creation.
- Made one `ApplicationRuntime` instance single-use and stop idempotent, preventing repeated start from orphaning a supervisor task against closed storage.
- Classified unexpected canonical-ingestion exceptions as worker-global lifecycle failures while preserving stale-lease exits and explicit ingestion rejection as action-local.
- Capped malformed-action quarantine at 100 rows per queue claim so the event loop and competing SQLite work regain control between batches.
- Validated retained attempt ordinal storage class and range before leasing, so real infinity, exponent text, zero, negative, or exhausted ordinals terminalize once instead of wedging the worker.
- Persisted store-owned final-transaction time as semantic action start and cooldown authority; caller timestamps no longer extend or backdate refresh policy.
- Required a successful later canonical ingestion before a persistence outage can recover, keeping one continuous outage disabled and represented by one event.
- Preserved object time ordering when an accepted observation predates local object creation, including a real WSL wall-clock regression and delayed-presence counterprobes.
- Removed two generated cross-shell virtual environments after exact path verification and added `.venv-wsl/` to ignore rules so local WSL execution cannot contaminate source archives.

## Evidence and metrics

- Windows full gate: 614 passed, 8 expected platform skips.
- Windows coverage: 90.25% statements, 76.57% branch-only, 86.93% combined.
- Ubuntu/WSL full gate: 616 passed, 6 expected platform skips.
- Ubuntu/WSL coverage: 87.59% statements, 75.31% branch-only, 84.61% combined.
- Format, standard lint, security lint, lock validation, and locked dependency audit pass.
- Windows and WSL distribution verification pass at 90 source-distribution entries, 66 wheel entries, and 34 installed runtime assets.
- Current candidate SHA-256 after the final WSL rebuild: sdist `14589A6AA91A214CC51BB1F5D4C6A1492D93AF59D7AEBE44160FB2EAFB6AB37B`; wheel `C3A4736DF17CB8CEEFA976AF69E0CF9213AB2C12AF6765336D1D6306C056B59D`; runtime constraints `A68A3A853DCC1F17527EDEF0C43000C191EB78B259F09EBFA01BFFBCD2B2A01C`.
- One initial Windows standalone-environment rename received transient access denial; an immediate isolated rerun passed. This remains a flake watch item rather than a claimed product defect.

## ETA and next work

The current repair batch is implementation-complete, independently closed, and committed as `b959697`. The next review slice will address the documented durable-state growth/headroom risk and any release-verifier flake evidence that survives repetition. Hourly progress and Murmuration tending continue through the deadline.
