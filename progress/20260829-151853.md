# Progress report - 2026-08-29 15:18 ET

## Summary

This hour closed the high oversized-collection defect and the medium Unity Catalog identity defect, then moved through three adversarial rounds on queue selection. Two major commits landed. The committed gate is green at 378 tests and 88% branch coverage. Queue migration `0017` now separates time-gated rows from runnable ordering, repairs exact index predicates, and passes 93 storage/coordinator tests plus a bounded 50,000-row VM-step regression; final million-row re-review and the exact full gate remain active.

## Metrics

| Metric | Result |
|---|---:|
| Latest committed Windows full-suite evidence | 378 passed, no warnings |
| Last committed Ubuntu/WSL parity | 364 passed, 88% |
| Branch coverage | 88%, CI floor 80% |
| Commits since 14:23 | 2 |
| Supported collection proof | 10,000 children in 41 deterministic parts |
| Multipart limits | 1,000,000 canonical bytes / 250 units |
| Queue focused evidence | 93 storage/coordinator tests |
| Future-deferred regression | 50,000 rows, fewer than 20,000 VM instructions |
| Tracked schema | `0016` committed; `0017` under review |
| Distribution | 80 sdist entries; 59 wheel entries; 28 installed runtime assets |
| Murmuration | 15:18 tend read-only; native project profile still unavailable |

## Completed

- Completed dual architecture review and adversarial implementation review of deterministic adapter-side multipart delivery.
- Added shared canonical observation-batch bytes, canonical unit ordering, byte/unit limits, versioned part IDs, per-part heartbeats/yields, and aggregate worker completion (`ce23fbe`).
- Kept child facets and `contains` authority indivisible, published the root summary last, retained incomplete coverage on every part, and rejected multipart complete/absence semantics before any write.
- Closed the single-batch fast-path hole: 251/600-item collections split despite remaining under the byte cap, and reordered sub-cap replay is digest-identical.
- Preserved previously valid large individual units below storage's hard limit while failing oversized indivisible UC units closed.
- Added typed, strict `schema_id`/`table_id`/`volume_id` identities, leaving witness-less legacy keys separate (`2d8e2ec`).
- Derived typed schema dispatch from the canonical schema leaf plus exactly one canonical catalog-parent relationship, not mutable facet names.
- Added and repaired a forced reverse-relationship index; malformed IDs, wrong source kinds, and ambiguous parents fail closed.
- Proved same-ID rename, same-name recreation, table/view/volume behavior, legacy separation, payload retention, and corrupt-facet dispatch resistance.
- Implemented queue claim migration `0017`: immutable intent order keys, runnable-only claim indexes, due-time promotion indexes, forced plans, and exact DDL/predicate repair.
- Removed the first queue revision's future-gated prefix scans by promoting due deferred/lease/retry rows before runnable selection; split NULL/time deferred seeks after review found an `OR` scan.
- Completed the hourly Murmuration tend. No scoped context appeared and credential-gated surfaces remained untouched.

## Validation and review

The committed collection and identity slices are independently clear. Collection normalization scales consistently rather than quadratically, the real 10,000-child worker/SQLite path retains every object and edge, and multipart complete coverage remains impossible. Unity Catalog review reproduced stable rename/recreation behavior and bounded canonical-parent dispatch. Queue review has already closed global sorting and wrong-predicate repair; its final future-prefix round uses explicit equality/range seeks plus VM-step evidence. No live Databricks profile or inventory was used.

## Active work and ETA

Queue migration `0017` is the active gate and remains uncommitted until the million-row re-review clears. The last queued medium is retained-history scanning for current cooldown/dashboard action state. After queue clearance: exact full/package gate, commit, real-database backup/migration, then the action projection. Work continues through Sunday 22:00 ET. The next tend and progress report are due around 16:19 ET.
