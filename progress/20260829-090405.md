# Progress report - 2026-08-29 09:04 ET

## Summary

This hour completed durable membership boundaries, full SQLite application/schema identity, dependency and CI supply-chain hardening, and complete installed-distribution verification. Windows is current at 333 tests and 88% branch coverage. Four focused commits landed; two release items remain active: reproducible isolated build dependencies and standalone wheel first-run guidance.

## Metrics

| Metric | Result |
|---|---:|
| Tests | 333 passed, no warnings |
| Branch coverage | 88%, CI floor 80% |
| Commits since 08:03 | 4 |
| Coordinator branch coverage | 94% |
| CLI commands | 5 |
| HTTP routes | 11 plus static mount |
| Current Windows test runtime | 14.41 seconds under coverage |
| Last Ubuntu/WSL parity | 331 tests, 88%, clean audit |
| SQLite | Migration `0015`; application ID `ROOK`; integrity `ok`; 0 FK violations |
| Distribution | 76 sdist entries; 57 wheel entries; 26 installed runtime assets |
| Locked audit | 27 packages, no known vulnerabilities |
| Murmuration | 08:40 tend read-only; native profile still unavailable |

## Completed

- Persisted relationship-authoritative complete-membership watermarks and backfilled valid legacy coverage (`accbc46`).
- Reconciled already-stale legacy edges during migration and skipped unknown, cross-system, wrong-type, corrupt-configured, or null relationship targets without FK failure.
- Proved restart persistence, strictly older suppression, equal/newer restoration, runtime pre-journal rejection, migration concurrency, and legacy upgrade safety.
- Added SQLite application ID `ROOK` and migration-derived initial/current schema fingerprints (`2ef9307`).
- Full schema validation now covers table/column names, declared types, nullability, primary keys, CHECK/FK/UNIQUE constraints, expression/partial indexes, and explicit index DDL.
- Rejected foreign markers, unrelated table/view schemas, incomplete initial/current schemas, and unknown future ledger versions without mutating marker, ledger, or sentinel data.
- Upgraded pytest from vulnerable 8.4.2 to 9.1.1, pinned uv 0.12.1, and added clean locked dependency auditing on both CI operating systems (`cccf1fe`).
- Replaced mutable GitHub Action tags with official immutable SHAs and enabled weekly Dependabot updates for action pins.
- Replaced the three-file wheel spot-check with a source-derived 26-asset exact manifest and isolated installed-wheel/resource/CLI smoke (`420a6aa`).
- Required every asset directory to exist/nonempty, exact versioned archive names, one archive pair, no missing/unexpected resources, no checkout import leakage, and bounded subprocesses.
- Expanded production security lint to scripts and added regressions for missing `app.js`, missing asset directories, unexpected assets, and both pytest launcher forms.
- Applied migration `0015` and the `ROOK` marker to the ignored real database after a recoverable backup; integrity and foreign keys remain clean.
- Completed the 08:40 Murmuration tend; no scoped public context appeared and all credential-gated surfaces remained untouched.

## Validation and review

Every consequential batch received independent skeptical review and at least one initial finding round. The exact Windows gate passes lock/audit, format, standard/security lint, 333 tests, 88% branch coverage, build, 26-asset archive validation, fresh wheel installation, packaged import provenance, and installed CLI help. The isolated Ubuntu/WSL gate is current through the dependency/CI batch at 331 tests and 88%; distribution changes are platform-structured and will receive refreshed parity after build constraints land. No live Databricks authentication or inventory was probed.

## Active work and ETA

Next are hashed constraints for isolated Hatchling/build dependencies and a complete first-run path for users installing the wheel outside a checkout. Work continues through Sunday 22:00 ET; the next Murmuration tend is due around 09:40 ET and the next progress report around 10:04 ET.
