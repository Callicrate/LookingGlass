# Progress report - 2026-08-29 13:23 ET

## Summary

This hour closed four independently reproduced medium defects across ingestion integrity, corrupt-queue recovery, browser failure UX, and operator recovery truth. It also added performance lint and bounded refresh-credit selection. Six focused commits landed. Windows and isolated Ubuntu/WSL pass the same 364-test, 88%-branch gate and the same 80/59/28 package verifier. Adapter-correctness and SQLite long-run audits continue with no known medium-or-higher finding open.

## Metrics

| Metric | Result |
|---|---:|
| Windows full-suite evidence | 364 passed, no warnings |
| Ubuntu/WSL full-suite evidence | 364 passed, no warnings |
| Branch coverage | 88%, CI floor 80% |
| Commits since 12:25 | 6 |
| Current Windows full-gate test runtime | 17.31 seconds |
| Current Ubuntu/WSL full-gate test runtime | 27.88 seconds |
| Collection-authority SELECTs, 100/200 items | 1,706 / 3,406 |
| CLI commands | 6 |
| HTTP routes | 11 plus static mount |
| Tracked and real SQLite | Migration `0016`; application ID `ROOK`; integrity `ok`; 0 FK violations |
| Distribution | 80 sdist entries; 59 wheel entries; 28 installed runtime assets |
| Murmuration | 13:22 tend read-only; native project profile still unavailable |

## Completed

- Added a repository-wide performance lint category and removed its one existing transformed-list finding (`a11fb40`).
- Preflighted observation IDs across both facet and relationship kinds so collisions cannot lend authority before their evidence is rejected (`28393f6`).
- Preserved exact duplicate replay, valid siblings, accepted-ID ordering, residue-free rejection, and linear collection-authority query growth under the collision repair.
- Made corrupt action/intent poison quarantine use bounded kind-separated digest keys rather than embedding malformed durable IDs (`20c1e12`).
- Made poison events tolerate dangling system references by retaining linkage only when the parent exists; overlong/dangling rows now terminalize once and release both queues while event/action linkage and rollback remain sound.
- Added a shared autoescaped Rookery error shell for browser 400/404/503 responses while preserving JSON-only intent polling, auth/Host order, security headers, status codes, and secret absence (`55e3137`).
- Verified the error shell in desktop and mobile Chrome with no overflow, correct landmarks/alert semantics, keyboard order, dashboard recovery, and safe retry links.
- Replaced `fetchall()` in latest refresh-credit selection with streaming first-match iteration after an audit measured 43.8 MiB at 50,000 rows (`38d8145`).
- Clarified that backup produces a consistent validated snapshot but restore is not implemented or verified; unsafe live replacement and SQLite sidecar mixing are explicitly prohibited (`c16c775`).
- Revalidated exact Windows and Ubuntu/WSL lock, audit, format, standard/security/performance lint, tests, coverage, constrained builds, and installed-wheel package contents.
- Completed the hourly Murmuration tend. No scoped forum context appeared; native identity, notifications, and BookStack remain unavailable, so no write was attempted.

## Validation and review

Every medium finding was independently reproduced, repaired, and re-reviewed. Collision probes covered cross-kind and differing-payload IDs; queue probes combined 600-character IDs with dangling foreign references and injected rollback; browser probes covered backend errors, stale links, malformed requests, hostile inputs, and API/browser content-type separation. The exact current full gate passes on Windows and Ubuntu/WSL at 364 tests and 88% branch coverage, and both constrained package checks include the new error template. No live Databricks authentication or inventory was probed.

## Active work and ETA

Independent reviewers are inspecting the remaining Databricks normalization/worker surface and supported-scale SQLite/query behavior. Any reproduced medium-or-higher finding will enter the same repair/re-review loop; otherwise work proceeds into lower-severity operational polish. Work continues through Sunday 22:00 ET. The next tend and progress report are due around 14:23 ET.
