# Progress report - 2026-08-29 06:12 ET

## Summary

This hour closed four medium defects: corrupt SQLite state escaping the CLI boundary, action-detail context loss, global deferred-scope policy wakeups, and self-authorized coverage/absence claims. It also made distribution contents explicit and CI-verified. Windows and Ubuntu/WSL are exact at 310 tests, 87% branch coverage, and migration `0013`.

## Metrics

| Metric | Result |
|---|---:|
| Tests | 310 passed, no warnings |
| Branch coverage | 87%, CI floor 80% |
| Commits since 05:12 | 20 |
| Coordinator branch coverage | 86% |
| CLI commands | 5 |
| HTTP routes | 11 plus static mount |
| Full Windows test runtime | 10.76 seconds |
| Full Ubuntu/WSL test runtime | 15.59 seconds |
| SQLite | Migration `0013`; integrity `ok`; 0 foreign-key violations |
| Distribution | 73 sdist entries; 55 wheel entries; verified on both OSes |
| Murmuration | 05:46 tend read-only; native profile still unavailable |

## Completed

- Updated the architecture flow so the final guard owns the atomic `leased → running` authority transition (`259f75f`).
- Removed hourly reports, live status, CI, and Murmuration metadata from source distributions (`f553f97`).
- Added a cross-platform archive verifier and made CI lint scripts plus inspect required wheel assets and forbidden sdist paths (`4585745`).
- Converted corrupt or incompatible SQLite failures into one generic CLI error and exit code 2 for `init`, `run-once`, and `serve`, with closed-handle proof (`5d24dc4`).
- Added validated local action-list return context to action-detail links, preserving page/state/system/action filters without open redirects or query injection (`53713f8`).
- Scoped policy-change wakeups to exact system/object and optional facet matches, including configured-scope aliases; migration `0012` adds required partial indexes (`41e1b43`).
- Persisted exact capability coverage policy—target kind, facet, refresh coverage, maximum completeness, and absence authorities—and enforced it deny-by-default in ingestion (`2f412fc`).
- Bound action-linked coverage to the action's exact capability/version; incidental batches require an explicit enabled capability, while already-running disabled actions may finish only under their own policy.
- Proved forged metadata `OBJECT_PRESENCE` authority is rejected and explicitly granted relationship/object authority remains functional.
- Applied migrations `0012` and `0013` to the ignored local database; integrity and foreign-key checks pass, and six configured capabilities now carry explicit non-empty policies.
- Revalidated exact Windows and Ubuntu/WSL gates plus archive contents at the 310-test head.
- Completed the 05:46 Murmuration tend; no scoped public context appeared and all credential-gated surfaces remained untouched.

## Validation and review

Every consequential slice received independent skeptical review. Several initial fixes were tightened after reviewers found cross-capability borrowing, disabled incidental authority, and query-plan scope issues. Current gates pass format, standard/security lint, lock validation, 310 tests, package builds, and archive-content verification on both operating systems. No live Databricks profile or credential was selected.

## Active work and ETA

No medium-or-higher defect is currently open. Work continues through Sunday 22:00 ET with fresh residual audits, exact cross-platform parity, and bounded operator/product improvements. The next Murmuration tend is due around 06:43 ET, and the next progress report is due by 07:12 ET.
