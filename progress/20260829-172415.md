# Progress report - 2026-08-29 17:24 ET

## Summary

This hour closed, committed, packaged, cross-platform verified, and real-migrated the final previously known medium: retained-history scans for cooldown/dashboard action state. A lower-severity badge polish also landed. Fresh post-migration audits then opened two new media: capability-neutral scopes crossing dedupe/cooldown identities and ambient Databricks auth/bundle precedence. The first is locally repaired with focused tests green and independent re-review running; the second is next.

## Metrics

| Metric | Result |
|---|---:|
| Latest exact full-suite evidence | 399 passed, no warnings |
| Windows and Ubuntu/WSL parity | 399 passed, 88% |
| Branch coverage | 88%, CI floor 80% |
| Commits since 16:22 | 4 |
| Projection scale | <1,000 VM steps at 1m history rows |
| Legacy history reads | 25–27m VM steps at 1m rows |
| Normal projection update | ~0.0006s, <1,000 VM steps |
| Capability repair focused evidence | 116 adapter/coordinator/composition tests |
| Tracked/real schema | `0018`; `ROOK`; integrity `ok`; 0 FK violations |
| Distribution | 82 sdist; 61 wheel; 30 installed runtime assets |
| Murmuration | 17:20 tend read-only; native project profile still unavailable |

## Completed

- Added migration `0018` with exact-scope cooldown anchors and bounded active/latest-terminal facet candidates (`c6ddbd0`).
- Backfilled derived state from durable action history and maintained it through database triggers inside existing transactions.
- Replaced cooldown MAX/history joins with one exact projection lookup and dashboard history grouping with bounded direct/configured projection reads.
- Extended authoritative DDL validation to triggers and added missing-trigger fail-closed behavior.
- Added conditional affected-group rebuilds for backward/NULL raw timestamp corrections while leaving normal lifecycle updates incremental and bounded.
- Independently benchmarked one-million-row projection reads at ~0.06–0.09ms and normal updates below one millisecond.
- Created `.local/backups/rookery-20260829-1647-pre-0018.sqlite3`, migrated the real database through `0018`, and verified three triggers plus projection/history equivalence (`a531806`).
- Revalidated the exact 399-test/88% gate and 82/61/30 package contents on Windows and Ubuntu/WSL (`3b75162`).
- Added semantic info/warning/critical/retry-wait/partial badge styles and a rendered/template regression (`7aef977`).
- Resolved one effective capability scope after selection for policy/evidence/dedupe/action authority while preserving nullable intent receipts.
- Added same-capability generic/explicit coalescing, legacy cooldown, active legacy action, legacy worker ingestion, and cross-capability provenance regressions.
- Completed the hourly Murmuration tend. No scoped context appeared; native identity, notifications, and BookStack remain unavailable, so no write was attempted.

## Validation and review

Projection `0018` is independently clear, real-migrated, and cross-platform green. Post-migration fault review also cleared multipart crash recovery, migration rollback, concurrent reclamation, WAL backup, poison handling, and current projections. Capability-neutral repair passes 116 focused tests; the last full run exposed only six old normalizer expectations after scopes became explicit, and those expectations plus the cross-capability regression are now focused-green. A fresh exact full gate remains pending final capability re-review. No live Databricks profile or inventory was used.

## Active work and ETA

Capability-neutral scope identity is the active gate. After independent clearance: exact full/package gate and focused commit, then controlled CLI environment/neutral working-directory isolation for ambient Databricks precedence. Work continues through Sunday 22:00 ET. The next tend and progress report are due around 18:21 ET.
