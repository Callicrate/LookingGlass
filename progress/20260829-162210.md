# Progress report - 2026-08-29 16:22 ET

## Summary

This hour closed the durable queue-selection medium through four adversarial review rounds, committed migration `0017`, backed up and migrated the real database, and implemented the last known medium: bounded action-state projections. Two focused commits landed. The projection passes 398 tests at 88% branch coverage and package verification, but remains uncommitted pending independent million-history/trigger review.

## Metrics

| Metric | Result |
|---|---:|
| Current local full-suite evidence | 398 passed, no warnings |
| Last committed Ubuntu/WSL parity | 364 passed, 88% |
| Branch coverage | 88%, CI floor 80% |
| Commits since 15:18 | 2 |
| Queue future-backlog proof | 1,000,000 rows, constant-step claim |
| Queue simultaneous-due proof | 1,001 bounded transactions and 1,000 yields |
| Queue transaction cap | 1,000 promotions |
| Tracked/real schema | `0017`; `ROOK`; integrity `ok`; 0 FK violations |
| Projection focused evidence | 128 storage/coordinator/composition tests |
| Distribution with `0018` | 82 sdist; 61 wheel; 30 installed runtime assets |
| Murmuration | 16:22 tend read-only; native project profile still unavailable |

## Completed

- Finished migration `0017` with immutable intent ordering keys, runnable-only claim indexes, and dedicated due indexes (`4a0ac01`).
- Repaired exact partial-index predicates by validating canonical full DDL in addition to keys, DESC, collation, and partial status.
- Split NULL and timestamp deferred seeks after review found an indexed `OR` scan across future rows.
- Bounded due promotion at 1,000 rows per immediate transaction and withheld claims until every due candidate was represented, preserving global priority/FIFO.
- Moved bounded catch-up inside the lease methods and yielded between transactions so callers receive a real lease or true idle rather than a promotion sentinel.
- Independently verified one million future rows in constant work and one million simultaneous due rows across 1,001 transactions, with exact final ordering and real CLI/runtime caller behavior.
- Created `.local/backups/rookery-20260829-1540-pre-0017.sqlite3`, migrated the real database through `0017`, and verified application ID, integrity, foreign keys, and all six queue indexes (`70bf20c`).
- Chose trigger-maintained action projections after architecture review rejected history-wide indexing and application-only hooks.
- Added local migration `0018` with exact-scope cooldown anchors, active candidates, one latest terminal candidate per source target/facet, backfill, indexes, and atomic triggers.
- Replaced cooldown/history aggregation with one exact projection lookup and replaced dashboard history grouping with bounded direct/configured projection reads.
- Extended authoritative schema DDL validation to triggers and added migration-backfill, raw transition, cooldown, active/terminal, trace-boundary, and missing-trigger tests.
- Completed the hourly Murmuration tend. No scoped context appeared; native identity, notifications, and BookStack remain unavailable, so no write was attempted.

## Validation and review

Queue migration `0017` is independently clear, packaged, and applied to the real database behind a validated backup. Its exact full gate passes 397 tests. Projection migration `0018` passes the complete 398-test/88% gate plus 128 focused tests; traced cooldown/dashboard reads do not touch action history, and package verification includes the 30th runtime asset. Independent projection stress review is still active, so neither the projection commit nor real `0018` migration has been performed. No live Databricks profile or inventory was used.

## Active work and ETA

Action projection `0018` is the sole known medium gate. After clearance: repair any trigger/backfill finding, rerun exact Windows/package and Ubuntu/WSL parity, commit, create a pre-`0018` real backup, migrate/verify the real database, then launch a fresh residual audit. Work continues through Sunday 22:00 ET. The next tend and progress report are due around 17:22 ET.
