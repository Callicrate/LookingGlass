# Rookery progress report: 2026-08-30 06:11 ET

## Outcome

This hour repaired Murmuration model provenance, closed web/storage grammar drift, removed startup queue materialization, bounded authority cancellation, pinned Databricks execution to a selector-aware verified profile snapshot, and added crash-safe snapshot recovery. The integrated candidate is green on Windows and Ubuntu/WSL. One final independent re-review of a just-repaired recovery retry race is still running before commit.

## Metrics

| Metric | Result |
|---|---:|
| Windows full suite | 546 passed, 8 expected platform skips |
| Windows coverage | 90.77% statements, 76.77% branch-only, 87.40% combined |
| Ubuntu/WSL full suite | 548 passed, 6 expected platform skips |
| Ubuntu/WSL coverage | 89.25% statements, 76.25% branch-only, 86.12% combined |
| Focused final candidate | 309 passed, 7 expected platform skips |
| Locked dependency audit | 27 packages, no known vulnerabilities or adverse status |
| Distribution | 89 sdist entries, 66 wheel entries, 34 installed runtime assets |
| 10,000-intent authority cancellation | 0.32 s locally; reviewer measured 50,000 at 0.812 s |
| Real SQLite | `ROOK`; migration `0021`; integrity `ok`; 0 FK violations |
| Murmuration identity | Principal correct; model provenance corrected to `openai/gpt-5.6-sol` |

## Completed

- Corrected topic 40 posts 1 through 5 from underspecified `openai/gpt-5` provenance through explicit post 6 under the unchanged native Rookery principal; committed status evidence as `5feb8d0`.
- Replaced the vulnerable check-execute-check profile flow with a selector-aware route witness and a minimized child snapshot derived from one guarded parse.
- Preserved existing v1 fingerprints for host-only profiles while separating shared-host `workspace_id`, `account_id`, and Azure route selectors under a v2 digest.
- Added active-lock protection, ordinary secret-file cleanup, hard-exit orphan recovery, and cross-platform crash regressions. The final retry-after-active-owner-death race is repaired and under re-review.
- Removed full canonical queue materialization at startup with the same deterministic timestamp grammar used by Python recovery.
- Added migration `0021_intent_aggregate_indexes`; reviewer probes now show indexed, linear cancellation through 50,000 affected intents without a scope scan or temporary distinct tree.
- Aligned object/alert query keys with canonical contract grammar, preserved meaningful boundary spaces in name-prefix search, and render-tested truthful empty-state copy.
- Benchmarked exact 0017-to-current offline upgrades through 250,000 action scopes. The worst measured distinct-key case completed in 6.77 s with a 9.5 MiB working-set increase and 226.0 MiB final projection growth; architecture guidance records the host and limits.
- Created `.local/backups/rookery-20260830-055944-pre-0021.sqlite3`, upgraded the live store, and verified the backup at migration 0020 and live store at 0021.
- Closed an alternate coverage-report filename leak from source archives and rebuilt a clean 89-entry sdist.

## Review status and ETA

Startup scale and web behavior are independently clear. Selector-aware authority, profile minimization, worker provenance, and normal crash cleanup are independently clear. The original profile reviewer found one follow-on medium race where a surviving process cached an incomplete recovery scan; the exact two-process hard-kill repair is implemented and its focused tests pass. Final re-review is expected within minutes, followed by the exact gate, focused commit, review-ledger closure, and continued fresh critical review. The autonomous campaign remains active through Sunday at 22:00 ET.
