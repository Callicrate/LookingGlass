# Progress report - 2026-08-29 19:16 ET

## Summary

This hour closed and committed one deep Databricks subprocess-authority repair, one lifecycle medium, and four lower-severity contract/consistency findings. The project now isolates CLI auth/bundle/executable/work-root authority, cleans up cancellation during lifespan entry, gives every disabled refresh control an accessible reason, validates direct programmatic settings as strictly as TOML, defines ingestion lease linearization, and applies exact-expiry semantics consistently. Independent storage, web, CLI/runtime, and end-to-end lifecycle reviews now report no medium-or-higher finding.

## Metrics

| Metric | Result |
|---|---:|
| Latest exact full-suite evidence | 439 passed, 1 platform-specific skip |
| Windows branch coverage | 89% |
| Ubuntu/WSL branch coverage | 88% |
| CI coverage floor | 80% |
| Commits since 18:20 | 8 |
| Distribution | 82 sdist; 61 wheel; 30 installed runtime assets |
| Locked dependency audit | 27 packages, no known vulnerabilities |
| Real SQLite state | `ROOK`; migration `0018`; integrity `ok`; 0 FK violations |
| Real CLI probe | v0.298 doctor passed; authentication not probed |
| Murmuration | 19:16 tend read-only; native project profile still unavailable |

## Completed

- Isolated mapped and doctor subprocesses from ambient Databricks/bundle variables, checkout/ancestor bundle roots, implicit executable lookup, and redirected/permissive work roots (`ad968ec`).
- Set and verified current-user ownership plus protected current-user-only Windows DACLs; enforced POSIX `0700`; moved standard-profile and doctor limitations into shared setup guidance.
- Required a visible accessible reason for every disabled refresh option and rendered matching `aria-describedby` targets on both product pages (`a9920d6`).
- Reproduced and repaired cancellation between runtime background-task creation and lifespan-entry cleanup; the exact scheduler regression proves task reaping and SQLite closure (`59be9ed`).
- Applied path/type, loopback, bound, profile, Workspace-root, stable-ID, count, and uniqueness validation to direct settings construction (`8b69914`).
- Defined action-linked ingestion lease authority at the `BEGIN IMMEDIATE` transaction-entry check, including bounded expiry crossing and exact replay semantics (`088d828`).
- Added deterministic doctor tests for the v0.298 floor, malformed/old rejection, newer acceptance, and all required help surfaces (`18b6d20`).
- Aligned unused legacy `mark_running()` exact-expiry behavior with the production final dispatch guard (`f27aa25`).
- Completed independent residual audits across storage/migrations/scale, CLI/config/runtime, web/security/product, and end-to-end coordinator/action lifecycle with no remaining medium-or-higher finding.
- Completed the hourly Murmuration tend; no scoped project discussion appeared and credential-gated surfaces remained untouched.

## Validation and review

Exact committed behavior passes 439 tests plus one complementary OS-specific skip on both Windows and Ubuntu/WSL, with 89%/88% branch coverage. Lock validation, dependency audit, Ruff format, standard/security/performance lint, constrained builds, 82/61/30 distribution verification, Git integrity, the six-command CLI surface, a real local CLI doctor, and read-only real-database integrity all pass. Every consequential repair received a focused independent re-review. No live Databricks profile authentication or inventory was used.

## Active work and ETA

No medium-or-higher defect is currently open. Work continues through Sunday 22:00 ET with fresh residual reviews, exact-boundary probes, and bounded product/operator polish; there is no artificial completion ETA before that deadline. The next tend and progress report are due around 20:16 ET.
