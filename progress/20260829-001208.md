# Progress report - 2026-08-29 00:12 ET

## Summary

Four correctness and lifecycle fixes were independently reviewed and committed this hour: direct Workspace metadata now persists and earns refresh credit, delayed older evidence cannot hide newer relationships, compatibility checks reap failed or cancelled subprocesses, and rejected ingestion items no longer leave phantom identities or journals. Murmuration identity is now intentionally registered, with the first tending pass completed read-only. A bounded action-activity page is implemented and fully green, while a new medium-severity cross-port cookie finding has become the next security priority.

## Metrics

| Metric | Result |
|---|---:|
| Tests | 221 passed, no warnings |
| Branch coverage | 86%, CI floor 80% |
| Storage branch coverage | 84% |
| Databricks adapter branch coverage | 77% |
| Full local gate runtime | About 8 seconds |
| HTTP routes | 10 plus static mount after the active action-history slice |
| Action pages | 50 rows, state/system filters, exact-ID lookup |
| Action query plans | Four indexed recency shapes, no temporary sort |
| Local SQLite | Migration `0008` applied; integrity `ok`; 0 foreign-key violations |
| Package build | Migration `0008` and `actions.html` present in wheel |
| Murmuration | Identity tracked; forum pulse empty; write profile unavailable |

## Completed

- Committed direct Workspace metadata exact-scope coverage as `db459e0`.
- Proved changed metadata updates SQLite, completes the action/intent, and satisfies the next request without another CLI call.
- Prevented content freshness from being claimed while artifact bytes still have no persistence consumer.
- Committed monotonic complete-membership omission reconciliation as `4e91484`.
- Committed cancellation, timeout, and output-limit process cleanup for compatibility checks as `a56d7bf`.
- Committed item-level rollback for rejected absence and relationship observations as `a1dfc2f`.
- Kept the 502-object path fast by limiting savepoints to late-rejection paths.
- Intentionally registered `.murmuration/project.toml` as `fd9ebe7` and completed the first requested tending pass.
- Implemented a bounded `/actions` page, durable state/system/exact-ID filters, alert-to-action links, redacted diagnostics, pagination indexes, package coverage, and strict query validation.
- Corrected the dashboard snapshot label so refresh options are no longer misrepresented as completed actions.

## New security finding

The host-only session cookie is still shared across ports for `127.0.0.1`. A different local service can receive that cookie if the authorized browser visits the other port, then replay it to Rookery. The evidence-backed repair is a high-entropy, process-unique `*.localhost` browser hostname used as the sole trusted Host and activation origin, with cookie-jar and real-browser regressions.

## Remaining boundary

Action attempt detail remains write-only and is the natural follow-up after action discovery. Murmuration cannot post until the owner provisions the native project identity profile; no credentials were inferred. Live Databricks inventory remains intentionally credential-gated and was not attempted.

## Next actions

Finish independent review and browser QA for action activity, targeted within 30 minutes, then commit it. Immediately afterward, close the cross-port session leak with a process-unique browser host. The next hourly report is due by 01:12 ET; the next Murmuration tending pass is due around 00:46-01:00 ET.
