# Progress report - 2026-08-29 18:20 ET

## Summary

This hour committed the capability-neutral scope repair and closed the implementation, cross-platform gate, real local doctor probe, and first independent review cycle for ambient Databricks subprocess precedence. The review reproduced two additional media—permissive pre-created Windows work-root ACLs and setup guidance hidden from standalone users—and both are repaired. Windows and Ubuntu/WSL now match at 417 passed, one platform-specific skip, and 88% branch coverage. A separate web product/security audit found no medium-or-higher defect.

## Metrics

| Metric | Result |
|---|---:|
| Latest exact full-suite evidence | 417 passed, 1 platform-specific skip |
| Windows and Ubuntu/WSL parity | 417 passed, 1 skipped, 88% |
| Branch coverage | 88%, CI floor 80% |
| Commits since 17:24 | 1 completed; current security slice pending re-review |
| Databricks focused process boundary | 18 passed, 1 OS-specific skip |
| Web residual review | 88 web tests; no medium+ finding |
| Distribution | 82 sdist; 61 wheel; 30 installed runtime assets |
| Locked dependency audit | 27 packages, no known vulnerabilities |
| Real local CLI probe | v0.298 doctor passed; work root empty after cleanup |
| Murmuration | 18:20 tend read-only; native project profile still unavailable |

## Completed

- Committed effective capability scope resolution with same-capability generic/explicit coalescing and provenance-filtered legacy compatibility (`9116bf8`).
- Unified mapped observation and doctor process execution under the same bounded timeout/cancellation/output-limit cleanup path.
- Removed inherited `DATABRICKS_*` and `BUNDLE_*` overrides while retaining ordinary profile-home, proxy, trust-store, and cloud SDK settings.
- Removed implicit current-directory and relative `PATH` executable discovery; Windows permits only direct `.COM`/`.EXE` candidates.
- Moved every call into a fresh empty work directory beneath a per-user root and rejected all four CLI v0.298 bundle filenames across the complete ancestor chain.
- Rejected redirected state/work directories, enforced POSIX `0700`, and replaced Windows DACLs with one protected inheritable current-user full-control ACE on every use.
- Added a Windows regression that pre-creates both roots with `Everyone:F` and verifies protected SDDL no longer grants `WD`.
- Moved standard-profile, ambient-override, and non-authenticating `doctor` guidance into prerequisites shared by wheel and source installs.
- Ran the installed local CLI's real version/help doctor through the hardened runner; no authenticated inventory or profile was selected.
- Completed an independent web boundary/product review with no medium-or-higher finding; queued its strongest low accessibility polish.
- Completed the hourly Murmuration tend. No scoped project context appeared; native identity, notifications, and BookStack remain unavailable, so no write was attempted.

## Validation and review

Exact-head Windows and Ubuntu/WSL each pass 417 tests with one complementary platform-only skip and 88% branch coverage. Lock validation, dependency audit, Ruff format, normal/security/performance lint, constrained build, and 82/61/30 distribution/install verification are clean. The first independent Databricks review found no high/critical issue and its two medium findings are repaired; focused re-review is the only remaining commit gate. The actual Windows state and work roots now show only the current-user inheritable full-control ACE. Live Databricks profile/authentication and inventory remain intentionally untested.

## Active work and ETA

Focused re-review of the Windows ACL and shared setup corrections is running. On clearance, this security slice will be committed immediately; the next bounded polish is requiring a real accessible explanation for every disabled refresh control, followed by another residual audit. Work continues through Sunday 22:00 ET without an artificial local stop. The next tend and progress report are due around 19:20 ET.

## Post-checkpoint correction - 18:26 ET

Focused re-review found that a protected DACL still retained a potentially foreign directory owner. Rookery now sets the owner to the current-user SID in the same Win32 operation, queries it back, and requires `EqualSid` before use. The Windows regression verifies owner equality plus protected no-`Everyone` DACLs for both roots. Final re-review is clear; exact-head Windows remains 417 passed/1 skipped at 88%, while Ubuntu/WSL is 417 passed/1 skipped at 87% because the Windows-only security implementation is intentionally unexecuted there. The completed slice is committed as `ad968ec`.
