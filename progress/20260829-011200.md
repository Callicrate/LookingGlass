# Progress report - 2026-08-29 01:12 ET

## Summary

This hour turned Rookery's local operations trail into a navigable, secure, durable workflow. Bounded action activity and per-action attempt detail are implemented, browser-tested, and committed. Browser sessions are isolated on a process-unique hostname, retries now wait durably instead of hammering the CLI, and expired deadlines cannot dispatch. Malformed queued actions now terminalize once while healthy work continues in the same lease call; that final hardening slice is fully green and awaiting its focused commit.

## Metrics

| Metric | Result |
|---|---:|
| Tests | 235 passed, no warnings |
| Branch coverage | 86%, CI floor 80% |
| HTTP routes | 11 plus static mount |
| Full local gate runtime | About 9 seconds |
| Action activity | 50 rows/page; state/system/exact-ID filters |
| Attempt detail | Latest 100, chronological display, truthful total |
| Browser-host entropy | 128 bits per runtime |
| Retry delays | 1s timeout/transient, 5s rate limit, 30s maximum |
| SQLite | Migration `0008`; integrity `ok`; 0 foreign-key violations |
| Browser QA | Action and attempt pages passed; Lighthouse 100 |
| Murmuration | Second tend pass complete; native writes still unavailable |

## Completed

- Committed bounded durable action activity as `81f337a`.
- Added indexed state/system pages, exact action discovery, redacted diagnostics, and alert links.
- Corrected the dashboard tile that had labeled refresh controls as actions.
- Committed process-unique `*.localhost` browser isolation as `a022a89`.
- Proved cookies are absent from ordinary `127.0.0.1`, `localhost`, and unrelated local hosts.
- Committed durable one-attempt-per-lease retry scheduling as `8d2e94b`.
- Proved retry eligibility and attempt ordinals survive database reopen.
- Committed expired-deadline terminalization as `2d49d2e`.
- Preserved still-live coalesced scopes by detaching and requeueing them while expiring only truly expired scopes.
- Committed bounded per-action attempt detail as `bc2d504`.
- Linked dashboard alerts, full alerts, activity rows, intent receipts, and polling updates to protected action detail.
- Stored `NULL` for successful no-diagnostic outcomes and handled 101-attempt overflow truthfully.
- Completed the second requested Murmuration tending pass; the public pulse had no Rookery-specific context.

## Active hardening

Malformed persisted action contracts now fail once as redacted `adapter_contract_mismatch` events before lease mutation. The lease loop skips the poisoned row and returns the next healthy lease in the same call, preventing `run-once` from falsely declaring the queue drained. Storage and composition regressions prove healthy runner progress and no poison-action CLI call.

## Remaining boundary

Murmuration posting remains blocked by the unprovisioned native project profile. Live Databricks inventory remains credential-gated and was not attempted. The next local priority will come from a fresh residual review after the poison-action commit.

## Next actions

Commit poison-action hardening immediately, then run parallel residual reviews across lifecycle, storage, and product truth. The next progress report is due by 02:12 ET; the next Murmuration tending pass is due around 01:44-02:00 ET.
