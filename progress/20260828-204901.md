# Progress report - 2026-08-28 20:49 ET

## Summary

Configuration now behaves as desired state instead of an append-only authority leak. Existing cache identity is preserved through a compatibility mapping, removed entries are paused without deleting facts, and concurrent database startup no longer races non-idempotent migrations.

## Metrics

| Metric | Result |
|---|---:|
| Tests | 152 passed, no warnings |
| Focused config/storage/composition tests | 46 passed |
| Concurrent migration openers | 8 successful |
| Ruff, lock, package build | Pass |
| Independent diff review | No blocker |
| Git commits before this batch | 5 |

## Completed

- Added optional explicit `databricks.id` for stable identity across name/profile rotation.
- Added migration `0004_configured_system_identities` and one-time adoption of legacy UUIDs.
- Made TOML the desired enabled state for Databricks systems, bindings, capabilities, and scopes.
- Preserved cached objects and facts when an entry is removed or its Workspace root changes.
- Proved stale queued work is cancelled before remote execution.
- Constrained reconciliation so it cannot enable resources from another system kind.
- Rechecked each migration ledger entry after acquiring `BEGIN IMMEDIATE` and closed failed constructors.

## Next actions

Commit this reviewed correctness batch. Then add runtime component supervision with bounded recovery and begin bounding the dashboard read model for large inventories and histories.
