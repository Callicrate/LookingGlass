# Progress report - 2026-08-29 02:12 ET

## Summary

This hour hardened Rookery against delayed evidence, incompatible persisted requests, malformed action history, and misleading refresh state. Seven focused commits are complete. The current uncommitted slice fixes a newly reproduced coalescing defect: an expired request can no longer inherit a shared action's later success while another live request still authorizes dispatch. That repair passes the full local gate and is under independent review.

## Metrics

| Metric | Result |
|---|---:|
| Tests | 243 passed, no warnings |
| Branch coverage | 86%, CI floor 80% |
| Commits this hour | 7 |
| HTTP routes | 11 plus static mount |
| Full local test runtime | 9.82 seconds |
| SQLite | Migration `0009`; integrity `ok`; 0 foreign-key violations |
| Native CI parity | Windows and Ubuntu/WSL passed at committed head |
| Package verification | Lock valid; wheel and source distribution build |
| Murmuration | 01:43 pass read-only; native profile still unavailable |

## Completed

- Committed malformed-action terminalization as `e71ae88`; poisoned rows fail once and healthy work continues.
- Committed object-presence timestamp monotonicity as `416d152`; delayed absence and presence cannot overwrite newer truth.
- Committed truthful facet refresh state as `8e145d1`; cached values now distinguish current, due, refreshing, and failed work.
- Committed incompatible-intent terminalization as `fd61cca`; persisted contract poison no longer blocks valid coordination.
- Committed canonical-ID presence revival as `9f16585`; canonical and external evidence now share the same watermark.
- Committed tolerant dashboard action summaries as `beca19d`; malformed latest action contracts no longer poison system status.
- Committed lock validation and package builds in both CI matrix jobs as `53ba294`.
- Revalidated the exact Windows and Ubuntu/WSL CI sequences at the committed head.

## Active hardening

A skeptical lifecycle review reproduced a medium-severity audit defect: an expiring request coalesced behind a non-expiring action remained `coalesced`, then appeared `succeeded` when the shared action completed. The guard now prunes per-request expiry before dispatch, preserves the still-live request's authority, and keeps the expired receipt linked to the action for audit without overlaying its terminal state. Two vertical regressions cover durable scope state, aggregate completion, dispatch authority, and the rendered receipt. The full gate passes at 243 tests and 86% branch coverage; independent review is still running.

## Remaining boundaries

Murmuration posting, notifications, and BookStack remain credential-gated because the native project profile is not provisioned; no credentials were inferred. Live Databricks inventory also remains intentionally untested without a selected authenticated profile. Changelog creation remains inappropriate on default `main` without a release/version boundary.

## ETA and next actions

There is no artificial local exit ETA before the Sunday 22:00 ET deadline; work continues as a rolling hardening campaign. Immediate next actions are to resolve the independent review, commit the coalesced-expiry repair, tend Murmuration around 02:43 ET, and begin another residual-risk pass. The next progress report is due by 03:12 ET.
