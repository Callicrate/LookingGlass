# Progress report - 2026-08-28 20:18 ET

## Summary

The project now has a secure-by-default web host policy, reproducible repository conventions, and a CI workflow that installs from the lockfile and runs the same checks used locally.

## Metrics

| Metric | Result |
|---|---:|
| Tests | 130 passed |
| Ruff format | Pass, 36 files |
| Ruff lint | Pass |
| Lock validation | Pass |
| Workflow YAML parse | Pass |
| Composed server smoke | HTTP 200 on loopback, HTTP 400 for test host |
| Coverage snapshot | 89% total statements |
| Commits | 2 before this repository-hygiene batch |

## Completed

- Removed `testserver` from the public `create_app()` default host allowlist.
- Added `.editorconfig` and `.gitattributes` for consistent cross-platform source handling.
- Added locked GitHub Actions verification using `uv 0.9.5`.
- Added a README contents map and documented CI parity.

## Next actions

Commit this hygiene batch, then perform a final weakness-first review of the complete tree and verify the final clean worktree.
