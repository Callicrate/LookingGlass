# Rookery progress — 2026-08-30 14:21 ET

## Outcome this hour

The static-contract review closed five findings: one medium Windows suspended-child leak path and four low receipt, duplicate-key, profile-parser, and deadline-partition contract defects. The implementation is committed as `e668818`, the closed ledger as `694407f`, and the final source tree/package evidence is clean.

The next critical review is active at `critical-reviews/20260830-141800.md`, testing whether one malformed retained row can hide unrelated cached state or wedge later valid work without weakening identity, schema, authority, or lease fail-closed behavior.

## Metrics

| Measure | Current evidence |
|---|---|
| Windows full suite | 633 passed, 8 expected platform skips |
| Ubuntu/WSL full suite | 635 passed, 6 expected platform skips |
| Windows coverage | 90.36% statements / 76.81% branch-only / 87.06% combined |
| Ubuntu/WSL coverage | 87.70% statements / 75.54% branch-only / 84.75% combined |
| Pyright 1.1.411 diagnostic probe | 144 errors, down from 160; no ignores, casts, or partial gate |
| Package verification | 92 sdist entries / 68 wheel entries / 36 installed runtime assets on both OS families |
| Locked dependency audit | No known vulnerabilities or adverse project statuses in 27 packages |
| Commits since prior checkpoint | 3 focused commits |
| Murmuration | Native principal and exact `openai/gpt-5.6-sol` provenance verified on topic 40 post 13 |

## Material changes

- Replaced private asyncio/`Popen` handle access with a public-PID Win32 handle carrying only `PROCESS_TERMINATE | PROCESS_SET_QUOTA`; all ownership-setup failures now kill and await the suspended child while preserving control exceptions.
- Replaced `ConfigParser(default_section=None)` and dynamic `optionxform` assignment with a typed no-default sentinel and case-sensitive parser subclass; NUL config is rejected and parse failures retain no secret-bearing exception context.
- Made the durable receipt output contract admit the valid-but-unsupported `system` target without widening browser refresh input.
- Made duplicate coverage identity one shared eight-field type and changed expired/live linked-scope classification from quadratic tuple membership to one linear pass.
- Parsed lease/projection timestamps once and made impossible missing schema values explicit.
- Independently cleared Win32 rights/cleanup, profile inheritance/case/NUL/traceback behavior, exact-expiry equivalence, and output/input separation.

## ETA and next work

The active corruption-containment review should reach a first evidence-backed decision in roughly 45–90 minutes. After any accepted repair, the exact Windows/WSL and package gates will run again. The broader autonomous improvement campaign continues through the fixed 22:00 ET deadline; no live Databricks profile or remote inventory will be guessed.
