# Rookery progress — 2026-08-30 04:17 ET

## Status

The 03:02 authority/boundedness review is fully implemented. Verified remote authority, pre-reconciliation serve ownership, exact schema allowlisting, and one-batch priority-aware queue promotion are independently clear. Forward-cursor navigation is green in focused tests after its final residual corrections and awaits the original reviewer's narrow closure before commit.

## Progress since 02:45

- Replaced mutable profile-label identity with a configured SHA-256 fingerprint of the normalized CLI profile workspace host.
- Added `fingerprint-profile`; raw hosts and credentials remain in the CLI-owned profile, while the worker re-verifies the fingerprint before process creation and after output returns.
- Normalized HTTPS default ports, DNS root dots, IP/IDNA forms, and rejected malformed hosts; parser errors no longer retain secret-bearing traceback context.
- Prevented explicit-ID and no-ID legacy cache from being silently blessed as a newly verified authority.
- Added one private OS-level database serve lock and listener-first startup so failed same-port or alternate-port launches cannot reconcile live configuration.
- Required exact migration-derived schema identity, rejecting extra tables/indexes/views/triggers during immutable preflight while preserving legacy migration and required-index repair.
- Limited each intent/action claim to one 1,000-row due batch. Intent promotion applies strict priority before truncation and bounded eligibility aging within each priority.
- Replaced dashboard, containment, action, and alert page-number/OFFSET/count paths with forward keyset cursors and `limit + 1` lookahead.
- Added migration `0019_web_cursor_indexes`, canonical bounded cursor parsing, indexed name-prefix filtering, immutable-ID unfiltered object order, and no-count action-attempt lookahead.
- Explicitly labeled cursor navigation weakly consistent under concurrent commits and provided a first-page restart control.

## Metrics

- Last complete Windows gate: 528 passed, 8 documented platform/privilege skips.
- Current focused cursor/authority/schema/queue tests: green after the final residual patches.
- Ruff format, standard/security/performance lint: green before the final narrow cursor patches; exact rerun pending.
- Package before final cursor residuals: 87 sdist entries, 64 wheel entries, 32 installed runtime assets.
- Prior exact coverage remains Windows 91.49% statements / 77.57% branch-only / 88.16% combined and Ubuntu/WSL 89.82% / 76.99% / 86.75%; refreshed exact coverage is pending this commit candidate.

## ETA and next work

Approximately 20 minutes to independent cursor closure, focused commits, and the exact Windows package/coverage gate. Ubuntu/WSL parity follows immediately. The autonomous campaign continues through 22:00 ET after this report closes.

## Risks / limits

- Cursor pages are intentionally weakly consistent, not snapshot-isolated; concurrent inserts before a cursor require restarting at the first page.
- The placeholder all-zero authority fingerprint is safe but non-operational: remote dispatch fails until the operator copies the digest printed by `fingerprint-profile`.
- Live Databricks inventory, restore tooling, and authoritative multipart collection completeness remain explicitly deferred.
