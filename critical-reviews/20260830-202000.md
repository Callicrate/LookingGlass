# Critical review: cross-surface release-candidate consistency

Review mode: final-candidate technical and product consistency review

Started: 2026-08-30 20:20 ET

Package/code baseline: clean `437d337`; review-opening documentation head `3cd3287`

Status: closed

## Scope and decision

This pass compares the complete local release candidate across tracked source, Git history, clean verified bundles, live ignored SQLite state/backups, running processes/listeners, current-status/progress ledgers, installed CLI help/exit contracts, README/architecture claims, and rendered product state. It looks for contradictions and stale evidence rather than reopening every already-proved implementation detail.

No live Databricks profile will be guessed. Repository/source, local disposable probes, exact package bytes, and the live local database remain the relevant authorities.

## Review plan and TODO

- [x] Establish clean `437d337`, 697/698, 86 diagnostics, and 97/73/41 baseline.
- [x] Verify Git cleanliness/history and package bundle hashes against the exact head.
- [x] Verify live database identity, migration provenance, integrity, foreign keys, and backup boundary.
- [x] Verify no QA/verifier process, listener, temp publication, or source artifact remains active/tracked.
- [x] Reconcile README, architecture, CLI help/exits, current status, progress, and Murmuration claims.
- [x] Run independent cross-surface review and repair every surviving contradiction.
- [x] Run exact final gates, commit, and close.

## Claim ledger

| ID | Claim under test | Evidence required | Status |
|---|---|---|---|
| C1 | Git head and final verified bundle name/header identify the same source. | Post-closure clean status, rev-parse, manifest recomputation. | Closure criterion defined below |
| C2 | Live local state is current, healthy, provenance-bound, and recoverable only to the stated backup boundary. | Read-only SQLite and backup probes. | Verified |
| C3 | No review/test helper remains able to affect ports, processes, package source, or evidence. | Process/listener/tree and tracked/untracked inventory. | Verified after cleanup |
| C4 | CLI help/exits, README, architecture, and browser recovery name the same current workflows. | Installed/source help and wording matrix. | Verified |
| C5 | Metrics and review/Murmuration claims are current, reproducible, and do not overstate browser/remote evidence. | Ledger-to-command/source comparison. | Verified after historical qualification |

## Findings

### F1 — Medium: current-status history uses present-tense “current” and identity claims

The top snapshot reports current 697/698 metrics and active native identity, while the chronological evidence section still contains older “exact-current” metrics and present-tense statements that native identity is absent. A reader or machine extractor can quote incompatible states without reconstructing chronology.

Repair: explicitly label the evidence section a historical ledger superseded by the timestamped current snapshot; convert old identity/current phrases to “at that checkpoint” language. Preserve the detailed history without presenting it as present authority.

### F2 — Medium: progress says static debt is closed while retaining 86 diagnostics

The 20:20 checkpoint could be read as “zero debt” even though its metrics intentionally retain 86 accepted contract-typing diagnostics.

Repair: say the static-diagnostic review is closed with 86 explicit residual limitations, and distinguish archive entry counts from installed runtime assets.

### F3 — Medium: exact-head claim and newest clean bundle identify different commits

During the active review, clean HEAD `3cd3287` followed package/code bundle `437d337`. Package-authoritative bytes were unchanged, and bundle hashes recomputed exactly, but an exact Git-provenance claim cannot call those the same head.

Repair boundary: identify `437d337` as the package/code baseline during review, then generate a fresh clean atomic bundle only after the final review/status commit. The final C1 check must compare that directory name/header to final `HEAD` exactly.

### F4 — Medium: four synthetic review processes survived completed agent sessions

Process inventory found one disposable SQLite claim reproducer and three Uvicorn apps on 8899–8901 after their reviewers had completed. All exact PIDs/commands were inspected, terminated, and rechecked; no project Python/uv/Chrome process or QA listener remains.

This is review-orchestration residue rather than Rookery runtime behavior, but it invalidated C3 until cleaned. Final process/listener inventory is required after all reviewers stop.

### F5 — Low: workflow wording carries avoidable ambiguity

CLI help described `run-once` as one batch while the command actually runs a bounded number of cycles; architecture shorthand “Linux support” could be confused with supported Linux execution rather than the deferred Linux-over-SSH adapter. Wording now names cycles and the Linux-over-SSH adapter explicitly.

## Final evidence

- Git was clean at each sampled commit; tracked package authority is unchanged by workspace-only review/status files, and no non-excluded untracked source exists.
- The latest sampled atomic bundle recomputed exact sdist, wheel, and constraints hashes; top-level copies matched. Absence of a configured remote is explicitly not external-publication proof.
- Live SQLite is `ROOK`, migration 0027, integrity `ok`, zero foreign-key violations, and 27/27 exact packaged provenance rows: 26 `ledger_adopted`, one `executed`. Inventory contains three systems and six cached objects, with no action history.
- `.local/backups/rookery-20260830-172653-pre-0027.sqlite3` is `ROOK`, migration 0026, integrity `ok`, and foreign-key clean. Documentation continues to deny a supported restore workflow or downgrade claim.
- Final process inventory after reviewer shutdown reports zero project Python/uv/Chrome helpers and zero listeners on 8765, 8891, 8892, and 8899–8901.
- Source CLI help exposes the same 11 commands and exit semantics as README/installed smoke. Browser recovery preserves the same `serve` invocation and browser geometry/AT limits remain explicit.
- Windows passes 697/8 and Ubuntu/WSL passes 698/7; coverage remains 89.90/76.32/86.61 and 87.50/75.18/84.52 statement/branch-only/combined. Pyright is 86/0/0 after a no-suppression 144 → 86 reduction. Packages verify at 97/73/41.

## Closure and exact-head check

No finding remains open. Because recording an exact bundle hash in a tracked closure file would create a new Git head, C1 closes operationally: after this closure commit, run the clean constrained build/verifier once, require the atomically published directory and manifest `# commit=` value to equal `git rev-parse HEAD`, recompute all three listed hashes, and make no later tracked change. Any failure reopens this review.
